# Core dependencies
requests>=2.31.0
python-dotenv>=1.0.0

# Flask for Railway webhook
Flask>=3.0.0
gunicorn>=21.2.0

# Modal for serverless webhooks (optional)
modal>=0.63.0

# Optional: for testing and development
pytest>=7.4.0
